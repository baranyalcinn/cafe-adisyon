/**
 * UpdateNotifier — Stubbed for Tauri
 * Electron auto-updater has been removed.
 * Tauri updater plugin can be added later if needed.
 */
export function UpdateNotifier(): null {
  // TODO: Implement Tauri updater when tauri-plugin-updater is configured
  return null
}
